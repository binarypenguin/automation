---
- name: Refresh KDE application database
  ansible.builtin.command:
    argv:
      - "{{ '/usr/bin/kbuildsycoca6' if workstation_kbuildsycoca6.stat.exists else '/usr/bin/kbuildsycoca5' }}"
      - --noincremental
  register: workstation_refresh_plasma
  changed_when: true
  become: false
